#  for item/variavel de interação no objeto:
#       declarações
#  else:
#       declarações


for item in ['bus', 'car', 'aviao']:
    print(item)

# range(x) - ocorre o incremento até a posição x;

# range(x,y) - ocorre o incremento em uma faixa pr-e-determinada;

# range(x,y,z) - entre os valores x até y, com exclusão do y, ocorre o incremento de z;

var = list(range(5))

print(var)